name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    
    defaults:
      run:
        working-directory: ./FlagGuesser
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: List files for debugging
      run: |
        echo "Current directory:"
        pwd
        echo "Files in root:"
        ls -la
        echo "Files in FlagGuesser:"
        ls -la ./FlagGuesser || echo "FlagGuesser folder not found"
    
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
    
    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v3
      with:
        build-root-directory: ./FlagGuesser  # Указываем корень Gradle проекта
    
    - name: Cache Gradle dependencies
      uses: actions/cache@v4
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
          ./FlagGuesser/.gradle  # Кешируем gradle в папке проекта
        key: ${{ runner.os }}-gradle-${{ hashFiles('./FlagGuesser/**/*.gradle*', './FlagGuesser/**/gradle-wrapper.properties') }}
        restore-keys: |
          ${{ runner.os }}-gradle-
    
    # Grant execute permission for gradlew внутри папки FlagGuesser
    - name: Grant execute permission for gradlew
      run: chmod +x ./FlagGuesser/gradlew
    
    # Run unit tests из папки FlagGuesser
    - name: Run Unit Tests
      run: |
        cd ./FlagGuesser
        ./gradlew testDebugUnitTest --stacktrace
    
    # Upload test results
    - name: Upload test results
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: test-results
        path: ./FlagGuesser/app/build/test-results/
        retention-days: 7
    
    # Build Debug APK
    - name: Build Debug APK
      run: |
        cd ./FlagGuesser
        ./gradlew assembleDebug
    
    # Upload Debug APK
    - name: Upload Debug APK
      uses: actions/upload-artifact@v4
      with:
        name: app-debug-apk
        path: ./FlagGuesser/app/build/outputs/apk/debug/*.apk
        retention-days: 30
    
    # Build Release APK
    - name: Build Release APK
      if: github.ref == 'refs/heads/main'
      run: |
        cd ./FlagGuesser
        ./gradlew assembleRelease
    
    # Upload Release APK
    - name: Upload Release APK
      uses: actions/upload-artifact@v4
      if: github.ref == 'refs/heads/main'
      with:
        name: flagguesser-release-${{ github.run_number }}
        path: ./FlagGuesser/app/build/outputs/apk/release/*.apk
        retention-days: 90
    
    # Create GitHub Release
    - name: Create Release
      if: github.ref == 'refs/heads/main' && github.event_name == 'push'
      uses: softprops/action-gh-release@v2
      with:
        tag_name: v${{ github.run_number }}
        name: Release v${{ github.run_number }}
        files: ./FlagGuesser/app/build/outputs/apk/release/*.apk
        generate_release_notes: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
